# Flask application settings
SECRET_KEY=your_super_secret_flask_key_here
# FLASK_ENV=development # Optional: Flask specific, app.py runs with debug=True by default

# MQTT Broker Configuration
# Replace with your actual MQTT broker details
MQTT_BROKER_URL=broker.hivemq.com
MQTT_BROKER_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TLS_ENABLED=false

# Note: The application can also update MQTT settings (excluding SECRET_KEY) 
# via its API, which will then be saved to the .env file.
